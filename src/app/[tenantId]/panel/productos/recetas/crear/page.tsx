import { Text } from "@heroui/react";

/** Placeholder: el formulario de recetas se implementará aparte. */
export default function CrearRecetaPlaceholderPage() {
  return (
    <div className="p-6">
      <Text className="text-foreground-secondary">
        Creación de recetas — próximamente.
      </Text>
    </div>
  );
}
